name: Branch lifecycle

on:
    delete:
    pull_request:
        types: [closed]

env:
    INFRA_ID: ${{ github.head_ref }}

jobs:
    destroy:
        runs-on: ubuntu-latest
        steps:
        - name: Echo infra_id
          run: echo $INFRA_ID