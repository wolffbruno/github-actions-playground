name: Terraform apply

on:
  workflow_run:
    workflows: ["Origin Pipeline"]

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Echo
        run: echo "Triggered from another pipeline"
